name: Deploy to Hostinger via FTP

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create .env file
        run: |
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> .env
          echo "DB_USER=${{ secrets.DB_USER }}" >> .env
          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> .env
          echo "DB_PORT=${{ secrets.DB_PORT }}" >> .env
          echo "DB_HOST=postgres-db" >> .env
          echo "PORT=${{ secrets.PORT }}" >> .env
          echo "NODE_ENV=production" >> .env
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env
          echo "PGADMIN_DEFAULT_EMAIL=${{ secrets.PGADMIN_DEFAULT_EMAIL }}" >> .env
          echo "PGADMIN_DEFAULT_PASSWORD=${{ secrets.PGADMIN_DEFAULT_PASSWORD }}" >> .env
          echo "PGADMIN_PORT=${{ secrets.PGADMIN_PORT }}" >> .env

      - name: Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          protocol: ftps
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/data/**
            **/volumes/**
            **/dist/**
            **/.DS_Store
      
      - name: Deployment Instructions
        run: |
          echo "📦 ✅ Arquivos enviados com sucesso via FTP!"
          echo ""
          echo "⚠️  PRÓXIMO PASSO - Execute no servidor:"
          echo ""
          echo "1. Acesse o Terminal SSH no hPanel da Hostinger"
          echo "2. Execute:"
          echo "   cd ${{ secrets.FTP_SERVER_DIR }}"
          echo "   chmod +x deploy-docker.sh docker-deploy.sh"
          echo "   ./deploy-docker.sh"
          echo ""
          echo "🤖 AUTOMATIZAR (opcional):"
          echo "Configure um Cron Job no hPanel:"
          echo "   */10 * * * * cd ${{ secrets.FTP_SERVER_DIR }} && ./deploy-docker.sh >> /tmp/deploy.log 2>&1"
  
